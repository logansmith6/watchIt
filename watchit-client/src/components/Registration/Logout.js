import React from 'react'

const Logout = () => {
    return (
        <div>
            <h1>
                <button className="logout__button">Logout</button>
            </h1>
        </div>
    )
}

export default Logout;